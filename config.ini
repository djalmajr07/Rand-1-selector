[Database]
# Path to the SQLite database file used by both main.py and api.py
db_path = sampler_data.db

[Logging]
# Optional: Define logging level if needed later (e.g., INFO, DEBUG, WARNING)
# log_level = INFO
